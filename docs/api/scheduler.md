# Scheduler

The central class that manages the polling loop, job execution, heartbeats,
orphan recovery, and vacuum.

```{eval-rst}
.. autoclass:: pg_scheduler.Scheduler
   :members:
   :undoc-members:
   :show-inheritance:
```
